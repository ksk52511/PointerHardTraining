//
//  Pointer.c
//  PointerHardTraining
//
//  Created by a on 11/16/25.
//

#include "Pointer.h"

//void main(void)
//{
//    int i = 10;
//    int *ptr = &i;
//    *ptr = i + 2;
//    
////    int *ptr; 초기값을 주지 않았기 때문에 쓰레기 값으로 초기화 됨
//    
//    printf("변수 i의 주소는 %p이며\n", &i);
//    printf("변수 i, 메모리에 저장된 값은 %d이다.\n\n", i);
//    
//    // 변수 &ptr은 ptr의 메모리상의 주소값 0x16fdfee50
//    printf("변수 ptr의 주소는 %p이며\n", &ptr);
//    // ptr은 변수 i의 주소값에 해당하는 0x16fdfee5c가 저장 되어있음
//    printf("변수 ptr의 내부 값은 주소로 %p이며\n", ptr);
//    printf("변수 ptr가 가리키는 메모리에 저장된 값은 %d이다.\n", *ptr);
//}
